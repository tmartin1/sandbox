// Given a string of XML code, write a function that check to make sure the code is in good form, meaning
// that every open tag has an appropriate closing tag and that they are all in the correct order.
// Assume that the input is always a non-empty string. Your function should return a boolean.

//
var checkXml = function(str) {
  //
};
